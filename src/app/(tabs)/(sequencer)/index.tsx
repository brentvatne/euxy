// Wave 0: the Sequencer tab shows the primitives gallery placeholder. The real
// Sequencer (lanes, transport, playhead) lands in Wave 2. Route stays thin.
import Gallery from '@/components/gallery';

export default function SequencerScreen() {
  return <Gallery />;
}
